node_modules/
.env
.env.*
!.env.example
dist/
*.log
test-results-*.csv
submissions.csv

# signal-lab/ directory removed in WS2 Phase P6b (WP-14, WP-16-WP-19); patterns
# migrated to collection/**/ and acquisition/runs/*/ below — see
# WS2-MIGRATION-PLAN-R3.md §4/§6.
collection/**/out/
collection/**/*-raw.json
collection/**/*-results.json
collection/**/*-results-v*.json
collection/**/*-summary.json
collection/**/*.jsonl
collection/runs/*/cohort-manifest.json
collection/sources/companies-house/runs/

# ── Repository Governance Rule — operational runtime state (SLG-028) ─────────────
# Authoritative platform artefacts ARE committed (Organisation Registries,
# CAPPED-SIC-CODES.md, manifests, curated/accepted datasets). Operational runtime
# state and non-promoted generated artefacts are reproducible and are NOT committed:
#   • processing ledgers / resume checkpoints / retry / queue / worker state
#   • pilot snapshots and temporary/intermediate acquisition outputs
# Applies to all current and future Population Acquisition runs (FCA, SRA, …).
# Updated by WS2 WP-08 (P4-C2): population-acquisition/ moved to acquisition/;
# signal-lab/population-acquisition/ no longer exists, so the old patterns are
# replaced outright (unlike WP-07's collection/ patterns, there is no
# leftover signal-lab/population-acquisition/ directory to keep matching).
acquisition/runs/*/ledger.ndjson
acquisition/runs/*/snapshots/
acquisition/runs/*/*.checkpoint
acquisition/runs/*/*.checkpoint.*
acquisition/runs/*/*.resume
acquisition/runs/*/*.tmp
acquisition/runs/*/tmp/
